

<div>
  <h1>
    <%= image_tag @user.avatar.url(:thumb) %>
    <%= t(".users_recipes", username: @user.username) %>
  </h1>
  <% if current_user != @user %>
      <%= follow_button @user %>
  <% end %>

  <br>
  <br>

  <div>
    <fieldset>
      <legend>list of recipes</legend>
      <%= render @recipes %>
    </fieldset>
  </div>
</div>
